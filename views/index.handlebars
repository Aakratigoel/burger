<h1>Burgers that are not Devoured!</h1>

<ul>
   {{!-- #each is a handlebars method that allows us to iterate over an array the same way we would do it with a standard for loop in javascript --}}
   {{#each burgers}}
      {{#unless Devoured}}
     <li>{{this.Burger_Name}} </li>  
     <button value = "Devour"  class="devour_button" data-id="{{this.ID}}" data-newdevour="1">Devour It!
     </button>  
      {{/unless}}
  {{/each}} 
</ul>

<h1>Burgers that are not Devoured!</h1>

<ul>
   {{!-- #each is a handlebars method that allows us to iterate over an array the same way we would do it with a standard for loop in javascript --}}
   {{#each burgers}}
      {{#if Devoured}}
     <li>{{this.Burger_Name}} </li>  
      {{/if}}
  {{/each}} 
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgerId">Burger Name:</label>
    <input type="text" id="burgerId" name="name">
  </div>
  <button type="submit">Add A Burger</button>
</form>





